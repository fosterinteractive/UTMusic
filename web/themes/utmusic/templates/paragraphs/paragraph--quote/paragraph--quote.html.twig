{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   - id: The paragraph ID.
 *   - bundle: The type of the paragraph, for example, "image" or "text".
 *   - authorid: The user ID of the paragraph author.
 *   - createdtime: Formatted creation date. Preprocess functions can
 *     reformat it by calling format_date() with the desired parameters on
 *     $variables['paragraph']->getCreatedTime().
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}

{# {% if is_inside_carousel %}
  {% set main_image_data = {} %}
  {% set small_image_data = {} %}
  {% set carousel = 'true' %}
  
  {% if content.field_quote_image_main[0] %}
    {% set main_image_data = {
      'path': content.field_quote_image_main[0]['#media'].field_ed_image_image.entity.uri.value,
      'alt': content.field_quote_image_main[0]['#media'].field_ed_image_image.0.alt,
      'width': content.field_quote_image_main[0]['#media'].field_ed_image_image.0.width,
      'height': content.field_quote_image_main[0]['#media'].field_ed_image_image.0.height
    } %}
  {% endif %}

  {% if content.field_quote_image_small[0] %}
    {% set small_image_data = {
      'path': content.field_quote_image_small[0]['#media'].field_ed_image_image.entity.uri.value,
      'alt': content.field_quote_image_small[0]['#media'].field_ed_image_image.0.alt,
      'width': content.field_quote_image_small[0]['#media'].field_ed_image_image.0.width,
      'height': content.field_quote_image_small[0]['#media'].field_ed_image_image.0.height
    } %}
  {% endif %}
{% endif %} #}

{% include "@molecules/quote/quote.twig" with {
    directory: 'themes/utmusic',
    variation: content.field_quote_variation[0] ? content.field_quote_variation[0]['#markup'],
    image_main: content.field_quote_image_main[0] ? content.field_quote_image_main,
    image_small: content.field_quote_image_small[0] ? content.field_quote_image_small,
    icon: 'quotes',
    text: content.field_quote_text[0] ? content.field_quote_text,
    additional_info: content.field_quote_more_info[0] ? content.field_quote_more_info,
    source: content.field_quote_source_name[0] ? content.field_quote_source_name[0]['#text'],
    url: content.field_quote_button_url[0] ? content.field_quote_button_url[0]['#url'],
    label: content.field_quote_button_label[0] ? content.field_quote_button_label[0]['#text'],
    aria_label: content.field_quote_button_a11y[0] ? content.field_quote_button_a11y[0]['#text'],
  } only
%}

    {# carousel: carousel,
    main_image_data: main_image_data,
    small_image_data: small_image_data, #}
